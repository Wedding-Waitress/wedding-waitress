

# Reduce Vertical Padding on Edit & Share Buttons

## Change
Reduce the vertical padding on both the **Edit** and **Share** pill buttons from `py-1.5` (12px total) to `py-0.5` (4px total) to make them noticeably thinner/narrower in height.

## Files
1. **`src/components/GuestLookup/EnhancedGuestCard.tsx`** (line 189) -- Edit button: change `py-1.5` to `py-0.5`
2. **`src/pages/GuestLookup.tsx`** -- Share button: change `py-1` to `py-0.5`




## Plan: Update sidebar styling for My Events, Tables, Guest List items

### Changes in `src/components/Dashboard/AppSidebar.tsx`

1. **Background**: Change `!bg-green-500 hover:!bg-green-600` → `!bg-purple-100 hover:!bg-purple-200` (very light purple)
2. **Badge numbers (1, 2, 3)**: Change circle from `bg-green-500 text-white font-bold` → `bg-purple-100 text-black font-normal`
3. **"Start Here" badge**: Change `text-destructive font-bold` → `text-black font-normal`
4. **"Create" / "Add" labels**: Change `text-white font-bold` → `text-black font-normal`

Single file, ~5 line changes.




## Plan: Update sidebar styling for purple-highlighted items

### Changes in `src/components/Dashboard/AppSidebar.tsx`

1. **Add purple border** to the three highlighted items: Add `border border-purple-300 rounded-md` to the `isGreenItem` conditional class
2. **"Start Here" badge**: Change from `bg-white text-black` → `bg-green-500 text-white` (green tablet with white text)

Two line changes in the same file.


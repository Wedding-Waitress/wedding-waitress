
# Change "Guest Type" to "Guest Category" in PublicAddGuestModal

## Change (single file)

**File:** `src/components/GuestLookup/PublicAddGuestModal.tsx`

**Line 199:** Change the label text from `Guest Type` to `Guest Category`. The red asterisk and all other styling remain unchanged.


# Move Bulk Actions Bar Higher

## Change
Move the purple bulk actions bar up by changing `bottom-16` (4rem / ~64px) to `bottom-28` (7rem / ~112px) on line 31 of `src/components/Dashboard/GuestBulkActionsBar.tsx`. This shifts it up by approximately 3 row heights.

## Technical Detail
**File: `src/components/Dashboard/GuestBulkActionsBar.tsx`** -- line 31: change `bottom-16` to `bottom-28`.

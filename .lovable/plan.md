
# Increase Maximum Font Size Slider

## Change

In **`src/components/Admin/TemplateTextZoneEditor.tsx`** (line 301), the font size slider currently has `max={72}`. This will be changed to `max={150}` so admins can set text zones up to 150px.

Single-line change -- no other files affected.

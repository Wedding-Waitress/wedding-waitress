
The sitemap at `public/sitemap.xml` already uses `https://weddingwaitress.netlify.app` for all 31 URLs — this was completed in the previous turn. No `weddingwaitress.com` references remain in the file.

## Plan
No changes needed. I'll re-verify by scanning `public/sitemap.xml` for any lingering `weddingwaitress.com` strings, and if (and only if) any are found, replace them with `https://weddingwaitress.netlify.app`. Otherwise I'll confirm the sitemap is already correct.

## Out of scope
Any other file or content.
